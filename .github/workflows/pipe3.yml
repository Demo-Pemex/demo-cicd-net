name: "sustitucion de variables"

on:
  workflow_dispatch:
  
jobs:
  dev: 
    uses: Demo-Pemex/template-netframework/.github/workflows/reemplazo.yml@main    
    with:      
      ambiente: "dev"
    secrets:  
      secretos_json: '{"Clave1": "${{ secrets.SECRETO1 }}","Clave2": "${{ secrets.SECRETO2 }}"}'
  

  qa: 
    uses: Demo-Pemex/template-netframework/.github/workflows/reemplazo.yml@main    
    with:      
      ambiente: "qa"
    secrets:  
      secretos_json: '{"Clave1": "${{ secrets.SECRETO1 }}","Clave2": "${{ secrets.SECRETO2 }}"}'
