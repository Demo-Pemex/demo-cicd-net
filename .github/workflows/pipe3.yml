name: "sustitucion de variables"

on:
  workflow_dispatch:
  
jobs:
  iniciar: 
    uses: Demo-Pemex/template-netframework/.github/workflows/reemplazo.yml@main
    secrets: inherit      
    with:      
      ambiente: "dev"
      variables_json: '{"Clave1": "seba","Clave2": "${{ vars.VARIABLE1 }}"}'
